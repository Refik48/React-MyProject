import React from "react";

const InternalStyle = () => {
  const stil = { color: "orange", fontSize: "2rem" };

  return <div style={stil}>Insan somut birseyler gormek istiyor</div>;
};

export default InternalStyle;
